<?xml version="1.0" encoding="utf-8"?>
<!-- Generated by android-multi-flavor-google skill v1.3.0 -->
<!-- Template: AndroidManifest.google.xml (src/{{flavorName}}/) -->
<!-- ⚠️ Placeholders ({{...}}) must be substituted before use -->
<!-- ⚠️ 本文件放到 src/{{flavorName}}/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- FCM 通知权限(Android 13+ 必须显式声明) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <!--
            FCM Service 静态注册。
            intent-filter 已启用,Service 直接响应 com.google.firebase.MESSAGING_EVENT。
        -->
        <service
            android:name="{{packageName}}.google.{{appPrefix}}PushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- BEGIN attribution.appsflyer -->
        <!--
            [仅 AppsFlyer 方案需要] 安装归因广播接收器。
            默认 Adjust 方案无需添加,Adjust 通过 InstallReferrerClient 自动获取。
            exported="true" 是必须的,安装归因广播来自系统外部。
        -->
        <!-- AppsFlyer:单一源安装归因 -->
        <receiver
            android:name="com.appsflyer.SingleInstallBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.vending.INSTALL_REFERRER" />
            </intent-filter>
        </receiver>
        <!-- AppsFlyer:多源安装归因(Samsung/Xiaomi/Huawei 等厂商商店) -->
        <receiver
            android:name="com.appsflyer.MultipleInstallBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.vending.INSTALL_REFERRER" />
            </intent-filter>
        </receiver>
        <!-- END attribution.appsflyer -->
    </application>
</manifest>
